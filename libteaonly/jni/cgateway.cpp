#include "cgateway.h"

static bool parseParameters(std::string &uri, std::vector<std::string > &params) {
	return false;
}

int GatwayInterface(std::string &file, std::string &uri) {
	std::vector<std::string> params;
	parseParameters(uri, params);
	return 0;	
}

